.next
node_modules
functions/lib
functions/node_modules
.tools
*.log
# functions/src is hand-formatted in a deliberate one-line-per-function style that Prettier would
# reflow entirely. Excluded so `format:check` reflects the convention actually in use there.
functions/src
