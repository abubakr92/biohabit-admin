# Comma-separated list of browser origins allowed to call the API. Requests from any other
# origin get no CORS headers and are blocked by the browser. Server-to-server calls (no Origin
# header) are unaffected. Copy this file to functions/.env and set your deployed admin origin.
ADMIN_ORIGINS=https://admin.biohabit.app,http://localhost:3000
